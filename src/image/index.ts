import logo from "./logo_store.png";
import search from "./icon_search.png";
import no_results from "./no_result.png";

export const Images = {
  logo,
  search,
  no_results,
};

export type TypeImage = keyof typeof Images;
